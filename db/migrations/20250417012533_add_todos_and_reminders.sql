-- Create "reminders" table
CREATE TABLE "reminders" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 4294967296), "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP, "updated_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY ("id"));
-- Create "todos" table
CREATE TABLE "todos" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 8589934592), "text" character varying NOT NULL, "done_at" timestamptz NULL, "owner_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "todos_users_todos" FOREIGN KEY ("owner_id") REFERENCES "users" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "todo_owner_id" to table: "todos"
CREATE INDEX "todo_owner_id" ON "todos" ("owner_id");
-- Add pk ranges for ('reminders'),('todos') tables
INSERT INTO "ent_types" ("type") VALUES ('reminders'), ('todos');
